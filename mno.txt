
به بخش (منو) خوش اومدی

درباره ربات فقط کافیه بگی `چی بلدی`

آموزش ساخت ربات `bot`

- باارسال ( درباره گروه ) اطلاعات گروه دریافت میکنی

-قیمت انلاین دستور ( !anlaen )

لیست بازی ها `/games`

@Belectron_bot

